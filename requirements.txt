# Core
boto3==1.34.122           # AWS SDK để làm việc với S3
requests==2.32.3          # Gọi HTTP API (Vietstock, crawl news)
python-dotenv==1.0.1      # Load biến môi trường từ file .env

# Apache Spark for Big Data Processing
pyspark==3.5.0            # Apache Spark Python API
pyarrow==14.0.1           # Fast columnar data processing
delta-spark==3.0.0        # Delta Lake for ACID transactions

# Data processing (keeping pandas for small data compatibility)
pandas==2.2.2             # Xử lý dữ liệu nhỏ, CSV/JSON
numpy==1.26.4             # Toán học cơ bản
vnstock3==0.2.4           # Vietnam stock market data v3 (latest)

# Database
psycopg2-binary==2.9.9    # Kết nối PostgreSQL (nếu bạn lưu metadata DB)

# Logging & retry
tenacity==9.0.0           # Retry với backoff (API call)
loguru==0.7.2             # Logging đẹp và tiện

# Additional dependencies for vnstock
ipython==8.18.1           # Required by vnstock
matplotlib==3.9.4         # Charts & visualization

# Web Scraping & APIs
beautifulsoup4==4.12.2    # HTML parsing cho news scraping

# Text Processing for RAG (Phase 1 preparation)
textblob==0.17.1          # Basic sentiment analysis

# Data Quality (Phase 1 integration)
great-expectations==0.18.8  # Data validation framework

# Machine Learning (future use)
scikit-learn==1.3.0       # ML algorithms

# Vietnamese Text Processing (for RAG)
pyvi==0.1.1               # Vietnamese text processing

# S3 Integration
s3fs==2023.12.2           # S3 filesystem interface

# Spark additional dependencies
py4j==0.10.9.7            # Python-Java bridge for Spark
findspark==2.0.1          # Find Spark installation
seaborn==0.13.2           # Statistical visualization
beautifulsoup4==4.12.3    # HTML parsing for news crawling
textblob==0.17.1          # Text processing & sentiment analysis

# Airflow (sau này orchestration)
apache-airflow==2.9.2     # Core Airflow
apache-airflow-providers-amazon==8.18.0   # Airflow + AWS S3
apache-airflow-providers-postgres==5.10.0 # Airflow + PostgreSQL

# (Tùy chọn: crawl news nâng cao)
beautifulsoup4==4.12.3    # Parse HTML
lxml==5.2.2               # Parser nhanh hơn cho BeautifulSoup
